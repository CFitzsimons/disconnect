{
  "name": "disconnect",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsc ./index.ts --outDir src && cp -R configuration src/ && node src"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^7.0.0",
    "typescript": "^2.1.5"
  },
  "dependencies": {
    "discord.js": "^11.0.0"
  }
}
